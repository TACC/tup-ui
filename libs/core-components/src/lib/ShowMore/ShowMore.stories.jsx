import ShowMore from './ShowMore';

export default {
  component: ShowMore,
  title: 'ShowMore',
};

const Template = (args) => <ShowMore {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
