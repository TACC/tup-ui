import TABLE_MARKUP from './SectionTableWrapper.test';

export default {
  component: TABLE_MARKUP,
  title: 'TABLE_MARKUP',
};

const Template = (args) => <TABLE_MARKUP {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
