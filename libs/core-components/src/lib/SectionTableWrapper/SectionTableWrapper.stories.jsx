import SectionTableWrapper from './SectionTableWrapper';

export default {
  component: SectionTableWrapper,
  title: 'SectionTableWrapper',
};

const Template = (args) => <SectionTableWrapper {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
