import HistoryBadge from './HistoryBadge';

export default {
  component: HistoryBadge,
  title: 'HistoryBadge',
};

const Template = (args) => <HistoryBadge {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
