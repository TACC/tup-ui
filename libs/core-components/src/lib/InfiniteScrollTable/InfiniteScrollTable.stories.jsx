import InfiniteScrollTable from './InfiniteScrollTable';

export default {
  component: InfiniteScrollTable,
  title: 'InfiniteScrollTable',
};

const Template = (args) => <InfiniteScrollTable {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
