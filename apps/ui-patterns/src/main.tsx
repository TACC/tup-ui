import React from 'react';
import ReactDOM from 'react-dom/client';
import UIPatterns from './app/UIPatterns';
import './index.css';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <UIPatterns />
  </React.StrictMode>
);
